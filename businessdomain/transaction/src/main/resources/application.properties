# swagger-ui custom path
#springdoc.swagger-ui.path=/swagger-ui.html
# Disabling the swagger-ui
#springdoc.swagger-ui.enabled=true

#Port the micro service
server.port=8095

#Application name
spring.application.name=apicontroltransaction

#Get configurations the Config Server
spring.cloud.config.enabled=true

#Access the configuration server
spring.cloud.config.profile=dev

eureka.client.serviceUrl.defaultZone=${EUREKA_SERVER:http://localhost:8761/eureka}
eureka.client.register-with-eureka=true
eureka.client.healthcheck.enabled=true
eureka.client.fetch-registry=true

#Security
#spring.security.user.name=admin
#spring.security.user.password=qwerty

management.endpoints.web.exposure.include=*

#Configuration the path Api, after the domain
#server.servlet.context-path=/business